# ⚠️ Superseded — use the shared backend

This per-app analytics script has been replaced by the **shared** Apps Script that serves all three apps (logging **and** email notifications on contact/sample forms).

👉 Set it up from **`Apps/_shared/`** (`_shared/Code.gs` + `_shared/README.md`). One deployment, one URL, pasted into all three apps.

(The old `Code.gs` in this folder is kept only for reference and should not be deployed.)
